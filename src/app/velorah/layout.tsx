import "./velorah.css";

/* `/velorah` — шилэн кино hero. CSS-ийг зөвхөн энэ мөчлөгт ачаална. */

export default function VelorahLayout({ children }: { children: React.ReactNode }) {
  return children;
}

import "./noir.css";

/* `/noir` — кино маягийн харанхуй theme. CSS-ийг зөвхөн энэ мөчлөгт
   ачаална (бусад хуудасны багцад ороогүй). */

export default function NoirLayout({ children }: { children: React.ReactNode }) {
  return children;
}

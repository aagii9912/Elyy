# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.next-verify/
/out/

# production
/build

# misc
.DS_Store
*.pem

# admin (local dev store — Supabase-гүй үед)
/.data
/public/uploads

# нууц түлхүүр / тохиргооны script (git-д ХЭЗЭЭ Ч бүү оруул)
.sa-key*.json
*service-account*.json
.set-vercel-env.sh

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Дизайн canvas — угсарсан гаралт (~2MB засварлагчийн багц агуулна).
# Эх артбордууд (.dc.html) болон canvas.json нь git-д хадгалагдана,
# гаралтыг нь тэднээс дахин угсардаг (design skill).
/.design/**/*.html
!/.design/**/*.dc.html
